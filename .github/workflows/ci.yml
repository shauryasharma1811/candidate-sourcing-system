name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: css_user
          POSTGRES_PASSWORD: css_pass
          POSTGRES_DB: css_test_db
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U css_user"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: pytest --maxfail=1 -q
        env:
          DATABASE_URL: postgresql+psycopg2://css_user:css_pass@localhost:5432/css_test_db
          JWT_SECRET_KEY: ci_test_secret
          S3_ENDPOINT_URL: http://localhost:9000
          S3_ACCESS_KEY: test
          S3_SECRET_KEY: test
          S3_BUCKET_NAME: resumes
          SMTP_HOST: localhost
          SMTP_USER: test
          SMTP_PASSWORD: test
          SMTP_FROM_EMAIL: no-reply@test.local

  frontend-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install
      - run: npm run lint
      - run: npm run build
